/*
package com.gobear.ignite.repo;

import com.gobear.model.UserInfo;
import org.apache.ignite.springdata.repository.IgniteRepository;
import org.apache.ignite.springdata.repository.config.RepositoryConfig;

import java.util.List;

@RepositoryConfig(cacheName = "UserInfoCache")
public interface IgniteUserRepo extends IgniteRepository<UserInfo, String> {

    List<UserInfo> getUserInfoByUsername(String username);

    UserInfo getUserById(String id);
}
*/
